# Codex Inspector VS Code Extension

This desktop extension provides Skills, Agents, MCPs, and Info views over the shared Codex Inspector core. Install the generated VSIX with `code --install-extension <file>.vsix`.
