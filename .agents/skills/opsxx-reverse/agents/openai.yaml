interface:
  display_name: "OPSXX Reverse"
  short_description: "Create a clean OpenSpec proposal from dirty worktree drift"
  default_prompt: "Use $opsxx-reverse to inspect staged and unstaged changes, reject ghost deltas, audit stale residue, and propose a reverse-engineered OpenSpec change."
