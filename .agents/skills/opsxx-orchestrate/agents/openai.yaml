interface:
  display_name: "OpenSpec Orchestrate"
  short_description: "Complete eligible OpenSpec changes in isolated worktrees"
  default_prompt: "Use $opsxx-orchestrate to process eligible OpenSpec changes in dependency order."
