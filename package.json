{
  "name": "codex-powertoys",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.30.0",
  "scripts": {
    "build-package-install": "pnpm run build && pnpm run package:cli && pnpm run package:vscode && pnpm run install",
    "install": "node -e \"const fs=require('fs'),path=require('path'),cp=require('child_process'); const dir='apps/vscode'; const files=fs.readdirSync(dir).filter(f=>f.endsWith('.vsix')).sort((a,b)=>b.localeCompare(a, undefined, {numeric: true, sensitivity: 'base'})); if(!files.length) process.exit(1); cp.execSync('code --install-extension ' + path.join(dir, files[0]) + ' --force', {stdio:'inherit'});\" && echo Restart VS Code",
    "build": "pnpm --filter @codex-powertoys/core build && pnpm --filter @codex-powertoys/cli build && pnpm --filter codex-powertoys-vscode build",
    "test": "pnpm --filter @codex-powertoys/core test && pnpm --filter @codex-powertoys/cli test && pnpm --filter codex-powertoys-vscode test",
    "check": "pnpm build && pnpm test",
    "package:cli": "pnpm --filter @codex-powertoys/cli build && pnpm --filter @codex-powertoys/cli pack",
    "package:vscode": "pnpm --filter codex-powertoys-vscode package"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.3",
    "typescript": "^5.9.2"
  }
}
