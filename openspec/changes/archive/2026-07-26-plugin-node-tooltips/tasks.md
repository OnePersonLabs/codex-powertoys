## 1. Tooltip Formatting

- [x] 1.1 Add a pure plugin tooltip formatter that labels plugin identity and path, preserves version/status metadata, and applies workspace-relative formatting only within the active workspace.
- [x] 1.2 Route plugin TreeItems through the formatter while retaining the existing plugin label, command, and resource identity behavior.

## 2. Coverage and Documentation

- [x] 2.1 Add formatter coverage for workspace-relative, global-canonical, and out-of-workspace plugin roots, plus source-level renderer coverage.
- [x] 2.2 Update README and resource-pane specification wording for source-aware plugin tooltips.
- [x] 2.3 Run the extension and repository build/test checks for the reverse-engineered patch.
